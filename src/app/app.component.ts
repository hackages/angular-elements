import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <h1>Let's play with Angular components</h1>
    <app-welcome></app-welcome>
  `
})
export class AppComponent {}
